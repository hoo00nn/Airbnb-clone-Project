mixin rooms(room={}) 
  .room__card
    .card__image
      img.room__img(src=room.image)
    .card__content
      .room__title=room.title
      .room__border
      .room__description=room.description1
      .room__option=room.description2
      .content__footer
        .room__reservation
          button.reservation.modal__open 예약하기
            .modal.hidden
              .modal__overlay
              .modal__content
                  //- +reservation({
                  //-   price:room.price,
                  //-   maxPerson:room.maxPerson,
                  //-   checkin: checkin,
                  //-   checkout: checkout,
                  //-   personnel: personnel
                  //- })
        .room__price
          span ₩#{room.price}
          span /1박
