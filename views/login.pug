doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title register
  body
    if id
      h1 #{id}님 안녕하세요
      form.logout__form(action="/auth/logout", method="POST")
        button(type="submit") 로그아웃
    else
      form.login__form(action="/auth/loginCheck", method="POST")
        input(type="text", name="id", placeholder="아이디")
        input(type="text", name="pw", placeholder="비밀번호")
        button(type="submit", name="login", value="login") 로그인
        button(type="submit", name="register", value="register") 회원가입
